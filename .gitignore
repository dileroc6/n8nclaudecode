.env
.env.*
!.env*.example
node_modules/
.next/

# Artefactos de deploy (zips de build) — no versionar
*_deploy_*.zip

# Proyecto anidado con su propio repo git (contiene .env.local con secretos)
Websites/Bejauha/plataforma/
